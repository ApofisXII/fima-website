{% extends 'admin/base.html.twig' %}

{% block title %} | Login{% endblock %}

{% block body %}

    <img class="login-logo" src="{{ asset('images/logos/fima-logo-blue.svg') }}" alt="Logo FIMA">
    <form method="post" class="gg-login-box gg-form">
        <h1>Accedi</h1>

        <label for="inputEmail">Email</label>
        <input type="email" placeholder="Inserisci email" value="{{ last_username }}" name="email" id="inputEmail" autocomplete="email" required autofocus>
        <br><br>
        <label for="inputPassword">Password</label>
        <input type="password" placeholder="Inserisci password" name="password" id="inputPassword" autocomplete="current-password" required>

        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

        <button class="gg-btn hoverizzato" type="submit">Accedi</button>

        {% if error %}
            <div class="gg-login-error">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}
    </form>

{% endblock %}
