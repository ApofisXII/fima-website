{% extends 'public/base.html.twig' %}

{% block title %}{{ 'news_list.page_title'|trans }} • {% endblock %}

{% block body %}

    <div class="container">
        <h1>{{ 'news_list.page_title'|trans }}</h1>

        <div class="news-grid">
            {% for news in newsList %}
                {% include 'public/_news-box.html.twig' %}
            {% endfor %}
        </div>
    </div>

{% endblock %}
